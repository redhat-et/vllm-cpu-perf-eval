name: embedding
description: Embedding model performance tests
runner: ansible
target: embedding-benchmark.yml

defaults:
  test_model: RedHatAI/granite-embedding-english-r2
  requested_cores: 16

param_mappings:
  model: test_model
  cores: requested_cores
