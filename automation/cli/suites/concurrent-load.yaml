name: concurrent-load
description: 3-phase concurrent load testing (baseline, realistic, production)
runner: ansible
target: llm-benchmark-concurrent-load.yml

defaults:
  base_workload: chat
  skip_phase_2: false
  skip_phase_3: false

param_mappings:
  model: test_model
  workload: base_workload
  cores: requested_cores
