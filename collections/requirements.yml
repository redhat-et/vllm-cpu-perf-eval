---
# Ansible Galaxy Collections Requirements
# AWX auto-installs collections from this file location
# Also available at: automation/test-execution/ansible/requirements.yml

collections:
  - name: containers.podman
    version: "1.9.4"
  - name: ansible.posix
    version: "1.5.4"
